# Apptly environment variables
#
# IMPORTANT: v1 is local-only and BYO-key users paste their API key into the
# extension UI (stored in browser.storage.local) - NOT here. This file only
# matters for the FUTURE hosted backend (Module 11) and its local development.
#
# Never commit a real .env file. Only this .env.example is tracked.

# --- Hosted backend (Module 11, not yet implemented) ---
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
